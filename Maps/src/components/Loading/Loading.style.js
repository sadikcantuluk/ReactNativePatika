import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    position: "absolute",
    bottom: 350,
    alignSelf: "center",
    backgroundColor: "white",
    padding: 15,
    borderRadius: 8,
  },
});
